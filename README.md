# A Contact Management System in C using the CLI
## This project is a means to help me learn about linked lists and apply the knowledge a project that is similar to the contacts app on phones

This project uses clean professional grade code with proper abstraction and comments for readability and speed.

Compile with...

``` bash

clang main.c functions.c -o main

```

and run with...

``` bash
./main
```
